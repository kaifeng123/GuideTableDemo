吧xlsx表转为json,或者byte

ini文件里里面直接填路径:
比如xlsx文件:XlsxPath=../table/     其中的table就是跟导表工具同级的文件名
导出文件的路径比如CSScriptPath导出cs脚本的路径:CSScriptPath=../../JsonDemo/Assets/   前面的../../不用管,后面的路径就是跟导表工具所在的父级文件夹同级开始的路径
